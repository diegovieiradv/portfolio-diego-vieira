export type Experience = {
  role: string;
  company: string;
  companyLink?: string;
  period: string;
  current?: boolean;
  summary: string;
  responsibilities: string[];
  technologies: string[];
};
